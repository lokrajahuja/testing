sudo: true
os: linux
cache: false
language: generic

git:
  depth: false

env:
- URL=https://appiriodx-website-prod.s3.amazonaws.com/downloads/adx-dev-cli-linux.tar.gz

before_install:
- mkdir adx
- cd adx
- wget $URL
- tar xzf adx-dev-cli-linux.tar.gz
- env
- export PATH=$(pwd):$PATH

#stages:
#- name : feature
 # if: branch = master
  #jobs:
 # - job: 
scripts:
  adx ci:shell -s dummy.sh
  
